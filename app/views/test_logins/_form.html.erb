<%= form_for(@test_login) do |f| %>
  <% if @test_login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_login.errors.count, "error") %> prohibited this test_login from being saved:</h2>

      <ul>
      <% @test_login.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
